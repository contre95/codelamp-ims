package contacts

type Contact struct {
	name  string
	phone string
}


//func (c *Contact) ValidateName() bool{
//	// Validates the name in the contact struct. Useful after parsing body from the http handler.
//}